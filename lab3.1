#include <stdio.h>
#include <math.h>
int main() {
    int f, r; 
    printf("Enter f r->");
    scanf("%d%d", &f, &r);
    if (f>=5)
    printf("%d", 3*f+2*r);
    else if (f<=-5)
    printf("%d", f*r-9);
    else 
    printf("%.3f", sqrt(8*r)/(2*f-4));

    return 0;
}
